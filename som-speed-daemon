#!/bin/bash
# /etc/init.d/som-speed-daemon

### BEGIN INIT INFO
# Provides:          som-speed-daemon
# Required-Start:    $syslog $named $network $time
# Required-Stop:     $syslog $named $network
# Default-Start:     2 3 4 5
# Default-Stop:      0 1 6
# Short-Description: som-speed.sm.de deamon
# Description:       The daemon for the codespeed instance for the SOM project
### END INIT INFO

DAEMON_PATH="/home/www-data/som-speed"

DAEMON=$DAEMON_PATH/speed_unicorn.sh
DAEMONOPTS=""

NAME=som-speed
DESC="The daemon for the codespeed instance for the SOM project"
PIDFILE=/var/run/$NAME.pid
SCRIPTNAME=/etc/init.d/$NAME

. $(dirname "$0")/daemon.inc
